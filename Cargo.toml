[workspace]
resolver = "2"
members = ["crates/wakeup", "crates/wakeup-core"]

# Shared release profile for every binary in the workspace.
[profile.release]
opt-level = "z"
lto = true
strip = true
panic = "abort"
